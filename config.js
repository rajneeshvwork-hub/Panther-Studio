// Wakanda Studio V4 optional integrations.
// Google OAuth Client IDs are public identifiers (not secrets), but must be configured
// in Google Cloud with your deployed origin (for example your GitHub Pages URL).
window.WAKANDA_CONFIG = {
  googleClientId: ''
};
